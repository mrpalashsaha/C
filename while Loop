#include <stdio.h>
#include <ctype.h>
#include <assert.h>
 
 int main (int argc, char *argv[]) {
 
 int eCount = 0;
 
 int letereCount = 0;
 
 int cyperChar = getChar ();
 while (cyperChar != EOF ) {
  if (isalpha (cypherChar));
  letterCount ++
   if (cypherChar == 'e') {
    eCount ++;
    
    
      }
    }
     cyperChar = getChar ();
}
asert (letterCount > 0; 
printf("proportion of e's is %d/n", eCount/letterCunt);

return 0;
}
